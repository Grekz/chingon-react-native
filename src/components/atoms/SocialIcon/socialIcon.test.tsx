/**
 * @format
 */
import 'react-native'
import React from 'react'
import { mount } from 'enzyme'
// import SocialIcon from '.'

describe('<SocialIcon />', () => {
  it.skip('should render svg correctly', () => {
    expect(mount(<SocialIcon />)).toHaveLength(1)
  })
})
