/**
 * @format
 */
import 'react-native'
import React from 'react'
import { mount } from 'enzyme'
// import Icon from '.'

describe('<Alert />', () => {
  it.skip('should render correctly', () => {
    expect(mount(<Icon />)).toHaveLength(1)
  })
})
